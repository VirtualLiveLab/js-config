name: Update Snapshot

on:
  workflow_dispatch:
    inputs:
      eslint:
        description: "Update ESLint Config Snapshot"
        required: true
        default: false
        type: boolean
      prettier:
        description: "Update Prettier Config Snapshot"
        required: true
        default: false
        type: boolean
      stylelint:
        description: "Update Stylelint Config Snapshot"
        required: true
        default: false
        type: boolean
  issue_comment:
    types: [created]

jobs:
  filter-pr:
    if: github.event_name == 'issue_comment' && github.event.issue.pull_request && github.event.comment.body == '/update-snapshot'
    runs-on: ubuntu-24.04
    outputs:
      eslint: ${{ steps.filter.outputs.eslint }}
      prettier: ${{ steps.filter.outputs.prettier }}
      stylelint: ${{ steps.filter.outputs.stylelint }}

    steps:
      - name: Check changed packages (PR Review Approved)
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        id: filter
        with:
          filters: |
            eslint:
              - 'packages/eslint-config/**'
            prettier:
              - 'packages/prettier-config/**'
            stylelint:
              - 'packages/stylelint-config/**'

  update-snapshot:
    needs: filter-pr
    runs-on: ubuntu-24.04
    env:
      ESLINT_CHANGED: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.eslint == 'true' || needs.filter-pr.outputs.eslint == 'true' }}
      PRETTIER_CHANGED: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.prettier == 'true' || needs.filter-pr.outputs.prettier == 'true' }}
      STYLELINT_CHANGED: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.stylelint == 'true' || needs.filter-pr.outputs.stylelint == 'true' }}
    if: ${{ github.env.ESLINT_CHANGED == 'true' || github.env.PRETTIER_CHANGED == 'true' || github.env.STYLELINT_CHANGED == 'true' }}
    permissions:
      contents: write
      pull-requests: write
    timeout-minutes: 10

    steps:
      - name: Checkout Repo
        uses: actions/checkout@eef61447b9ff4aafe5dcd4e0bbf5d482be7e7871 # v4.2.1

      - name: Configure Git Identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - run: corepack enable pnpm

      - name: Setup Node.js 20.x
        uses: actions/setup-node@0a44ba7841725637a19e28fa30b79a866c81b0a6 # v4.0.4
        with:
          node-version-file: "package.json"
          cache: "pnpm"

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Update ESLint Config Snapshot
        if: github.env.ESLINT_CHANGED == 'true'
        run: pnpm run test --project eslint-config -- -u

      - name: Update Prettier Config Snapshot
        if: github.env.PRETTIER_CHANGED == 'true'
        run: pnpm run test --project prettier-config -- -u

      - name: Update Stylelint Config Snapshot
        if: github.env.STYLELINT_CHANGED == 'true'
        run: pnpm run test --project stylelint-config -- -u

      - name: Create change summary markdown
        run: |
          set -eu
          echo "## 🚚 Updated snapshots" >> CHANGES.md

          if [ ${{ github.env.ESLINT_CHANGED }} == 'true' ]; then
              echo "- ESLint" >> CHANGES.md
          fi
          if [ ${{ github.env.PRETTIER_CHANGED }} == 'true' ]; then
              echo "- Prettier" >> CHANGES.md
          fi
          if [ ${{ github.env.STYLELINT_CHANGED }} == 'true' ]; then
              echo "- Stylelint" >> CHANGES.md
          fi

      - name: Commit and Push Changes
        if: github.event_name == 'issue_comment'
        run: |
          set -eu
          git add .
          git commit -m "chore: update snapshot (github-actions)"
          git push

      - name: Comment to PR
        if: github.event_name == 'issue_comment'
        run: |
          set -eu
          if [ ! -f CHANGES.md ]; then
            echo "::warn::comment body not found."
            exit 0
          fi

          gh pr comment ${{ github.event.issue.number }} --body-file CHANGES.md --edit-last \
            || gh pr comment ${{ github.event.issue.number}} --body-file CHANGES.md
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Create Pull Request
        if: github.event_name == 'workflow_dispatch'
        uses: peter-evans/create-pull-request@5e914681df9dc83aa4e4905692ca88beb2f9e91f # v7.0.5
        with:
          commit-message: "chore: update snapshot (github-actions)"
          title: "Update Snapshot"
          body-path: CHANGES.md
